{% extends "base-field" %}

{% set name="page-title" %}

{% block content %}

  {% if link is empty %}
    <h1>{{ title | raw }}</h1>
  {% else %}
    <h1><a href="{{ link.url }}">{{ title | raw }}</a></h1>
  {% endif %}

{% endblock %}
